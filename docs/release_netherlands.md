# Netherlands Branch Release Notes

Branch: `netherlands`. Status: internal validation only; `NL=false`.

Added source-verified Netherlands fundamentals, relational `NL` persistence,
CSV ingestion, deterministic Dutch/IFRS mappings, Euronext Amsterdam routing,
and quality gates. Public release remains blocked on AFM/issuer provenance,
cross-listing identity controls, source rights, coverage audit, and price-data
licensing.
